# Fichier de test
# Les lignes de commentaires commencent par #
procedure principale()
i=0;
si i==1)
ecrire("ca ne devrait pas s'afficher");
sinonsi (i==2)
ecrire("balec");
sinon
ecrire("jsp");
finsi
finproc
# Commentaire Final
